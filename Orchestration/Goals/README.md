# Goals Folder

This directory stores personal goal files consumed by the orchestration scripts.
They are treated as runtime data and intentionally ignored by git.

Feel free to create files such as `CurrentGoal.txt`, `MyGoal.txt`, or
`Phase1.txt`—they will stay local on your machine.
