# Base URL for your APIs (default matches LaunchUnifiedToolbox.ps1 backend port)
VITE_API_BASE=http://localhost:8000
# Optional GitHub token for authenticated calls
VITE_GITHUB_TOKEN=
# Override dev/preview host & ports if the defaults conflict on your machine
# VITE_HOST=127.0.0.1
# VITE_DEV_PORT=5173
# VITE_PREVIEW_PORT=4173
# VITE_PORT_SCAN_MAX=25
