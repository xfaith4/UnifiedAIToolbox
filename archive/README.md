# Archive

Legacy copies preserved during consolidation:

- `archive/apps-web-legacy` – early Next.js site (placeholder prompts/agents pages). Superseded by `apps/unifiedtoolbox.webapp`.
- `archive/project-dashboard-legacy` – older milestone dashboard (Vite/React). Superseded by `apps/dashboard` and the unified webapp.

If you need assets or history, reference these archived copies; new work should target the live apps above.
