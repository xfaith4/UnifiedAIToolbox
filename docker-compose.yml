version: '3.8'

services:
  prompt-api:
    build:
      context: ./apps/UnifiedPromptApp/services/prompt-api
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      - PROMPT_API_PORT=8000
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - FRONTEND_PORT=${FRONTEND_PORT:-5173}
      - WORKBENCH_PORT=${WORKBENCH_PORT:-8501}
    volumes:
      - ./data:/app/data:ro
      - ./apps/UnifiedPromptApp/services/prompt-api/data:/app/data-local
      - ./apps/UnifiedPromptApp/services/prompt-api/workbench.db:/app/workbench.db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  dashboard:
    build:
      context: ./apps/dashboard
      dockerfile: Dockerfile
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    environment:
      - VITE_PORT=5173
      - VITE_API_BASE=http://localhost:${API_PORT:-8000}
    depends_on:
      prompt-api:
        condition: service_healthy
    restart: unless-stopped

  unified-webapp:
    build:
      context: ./apps/unifiedtoolbox.webapp
      dockerfile: Dockerfile
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      - PORT=3000
      - NEXT_PUBLIC_API_BASE=http://prompt-api:8000
    depends_on:
      prompt-api:
        condition: service_healthy
    restart: unless-stopped

networks:
  default:
    name: unified-ai-toolbox
