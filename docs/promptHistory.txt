
You are working in the Unified AI Toolbox web app. Update the “Milestones” page (AI-Orchestration Milestone Dashboard) so the Insights section measures orchestration QUALITY, not just averages.

GOALS
1) Improve Insights with quality + reliability + efficiency analytics:
   - Outcome breakdown (Accepted/Refine/Rejected/Unknown) that sums to runsTracked
   - Percentiles (median/p75/p90) for tokens, cost, score
   - Correlation views: Score vs Cost, Score vs Tokens (scatter + outliers)
   - Time-series trends (rolling avg for score/cost/tokens)
   - Basic reliability metrics: duration p50/p95, retry count, error rate (if available)

2) Add per-run “Trace” details so a user can audit a run:
   - Show original prompt and effective prompt used by the orchestrator
   - Show agents used (ordered list, role, model if available)
   - Show step timeline/events with timestamps + tokens/cost per step (if available)
   - Link to artifacts via Run Path when present

3) Add feedback loop UI:
   - In run details, add a Feedback section:
     * thumbs up/down OR 1–10 rating (both ok)
     * editable tags
     * freeform note
   - Persist feedback locally alongside run data (prefer writing a small JSON file near the run or updating Milestone_Log.json via existing backend endpoint; if no backend write path exists, implement a simple localStorage persistence keyed by runId as a temporary MVP).
   - Add a “Generate refinement prompt” button that produces a ready-to-copy text block using:
     * goalPromptEffective
     * agent list summary
     * evaluation outcome/score
     * user feedback
     * and specific instructions for improving the next run

DATA / INGEST REQUIREMENTS
- Current source is MilestoneDashboard/public/data/Milestone_Log.json (or equivalent). Keep compatibility with existing entries.
- Extend the run schema to support:
  runId, timestampStart, timestampEnd, durationMs,
  goalPromptOriginal, goalPromptEffective,
  agentsUsed: [{name, role, model, order}],
  steps: [{t, agent, type, label, tokens, cost, status, artifactPaths}],
  evaluation: { outcome, score, rubric, successCriteria[] },
  feedback: { rating, thumbs, tags, note, createdAt }.
- If any fields are missing, UI must not crash. Show “Not captured for this run”.

BUGFIX / CONSISTENCY
- Fix the current outcome counters (e.g., “unknown:32” while accepted+refine already exist). Ensure the displayed counts are derived from actual run entries and sum correctly.

DELIVERABLES
- Code changes implementing the above on the Milestones dashboard:
  * Updated Insights components (percentiles, correlations, trends)
  * Updated Run Details view (tabs recommended: Summary / Prompt / Agents / Steps / Artifacts / Feedback)
  * Feedback persistence (best available option)
  * Generate-refinement-prompt output UI (copy button)
- Add/adjust any types/interfaces (TypeScript) and sample data updates for local dev.
- Add minimal tests or sanity checks if the project has a test setup.

CONSTRAINTS
- Keep existing look/feel (dark UI).
- No breaking changes for old logs.
- Prefer small, composable components.

Start by locating the Milestones page component and the JSON ingestion logic, then implement incrementally with clean interfaces and graceful fallbacks.

====================================================================
You are working in the Unified AI Toolbox web app, specifically the Milestones (AI-Orchestration Milestone Dashboard) page.

TASK
Add a “Compare Runs” feature that lets a user select 2 runs and view a clear, engineer-friendly diff across:
- Prompts (original + effective)
- Agent plan (agents used, order, role, model)
- Evaluation (outcome, score, rubric breakdown, success criteria)
- Steps/timeline (events, tokens/cost per step, status)
- Artifacts (paths/links), and feedback if present

UX REQUIREMENTS
1) Entry point:
   - Add a “Compare” button near the run list controls OR a persistent “Compare Runs” panel in the page header.
   - User can pick “Run A” and “Run B” from dropdowns (searchable by runId + timestamp + tags).
   - Also support quick selection from the run list: a checkbox or “Add to Compare A/B” action per run.

2) Compare view layout:
   - Use a 2-column side-by-side layout (Run A left, Run B right) with a center “Diff” column or inline highlights.
   - Provide a top summary card:
     * Score delta (B - A)
     * Cost delta
     * Tokens delta
     * Outcome delta (Accepted/Refine/etc.)
     * Duration delta (if present)
   - Tabs (recommended): Overview / Prompts / Agents / Evaluation / Steps / Artifacts / Feedback

3) Diff behavior:
   - For text (prompts, notes): show an inline diff or highlighted changes. If you don’t want a diff lib, implement a simple line-based diff:
     * split by newline
     * show removed/added lines with clear markers
   - For lists (agents, steps, criteria): match items by stable keys when possible (agent.name+order, step.label+agent+type, criteria.id). Show:
     * added items (only in B)
     * removed items (only in A)
     * changed items (present in both but different fields)

4) Usability:
   - “Swap A/B” button
   - “Clear selection” button
   - “Copy comparison summary” button that outputs a clean markdown/text summary (deltas + key diffs)

DATA + COMPATIBILITY
- Runs are loaded from Milestone_Log.json (or existing ingestion). Maintain backwards compatibility:
  - If any fields are missing, show “Not captured” and the compare view must still render.
- Use these optional fields when available:
  runId, timestampStart/timestampEnd/durationMs,
  goalPromptOriginal, goalPromptEffective,
  agentsUsed[], steps[], evaluation{}, feedback{}, artifactPaths/Run Path.

IMPLEMENTATION NOTES
- Add TypeScript interfaces for Compare selection state and a set of pure helper functions:
  - normalizeRun(run): ensure consistent shapes and defaults
  - diffText(a,b): returns added/removed/unchanged lines
  - diffAgents(a[],b[])
  - diffSteps(a[],b[])
  - diffCriteria(a[],b[])
  - diffArtifacts(a[],b[])
- Keep the diff logic deterministic and unit-testable (pure functions). Add minimal tests if the repo has a test framework.
- Keep styling consistent with the dark theme and existing components.

DELIVERABLES
- Compare Runs UI integrated into the Milestones page
- Compare view with summary + tabs + diffs
- Helper diff utilities + types
- Graceful fallbacks for missing data
- Basic sanity checks/tests if available

ACCEPTANCE CRITERIA
- User can select any two runs and see a stable compare view without crashes.
- Summary deltas compute correctly.
- Prompts and agent plan differences are easy to spot.
- Steps/criteria/artifacts show added/removed/changed items.
- Works on older runs with partial data.

##################################################################

You are working on the Milestones (AI-Orchestration Milestone Dashboard) page in the Unified AI Toolbox web app.

PROBLEM STATEMENT
The current analytics feel “chart-like” but not “decision-grade”:
- The three “distribution” horizontal bars span the full width and do not visually encode bucket counts or percentages, so they’re not interpretable.
- Several charts lack axis labels, units, and definitions (Score, Cost, Tokens, Reliability).
- Users don’t know how scoring is computed or how to interpret cost beyond a raw number.
We need to turn this into a real analytics dashboard that explains meaning, context, and recommended actions.

GOALS
1) Make the dashboard interpretable:
   - Every chart must have:
     * Axis labels (and units where applicable)
     * Tooltips showing exact values
     * Legends where needed
   - Add “Definitions / Methodology” UX:
     * A small “ⓘ How scoring works” link near Score KPIs
     * A “Glossary” modal/panel defining: score, outcome, tokens, cost, duration, retries, error rate, percentiles
     * A “Scoring rubric” breakdown view when rubric data exists (and clear fallback text if not captured)

2) Replace meaningless “distribution bars” with real distributions:
   - Replace each full-width distribution bar with a proper histogram or segmented stacked bar where:
     * Each bin segment width is proportional to % of runs in that bin
     * Each segment shows count and percentage on hover
     * Show markers for median and p90 on the distribution
   - Provide sane defaults for binning:
     * Tokens: dynamic bins (e.g., 6–10 bins based on range)
     * Cost: fixed precision bins (or dynamic bins but readable)
     * Score: bins by integer or 0.5 increments depending on range

3) Improve “storytelling” layout (dashboard structure):
   - Create 3 clear sections with headings and brief helper text:
     A) Health at a glance (KPIs)
     B) Distributions & outliers (histograms + “top outliers” list)
     C) Trends & drivers (time series + correlation)
   - Add a “Time range / window” selector:
     * All runs / last 7 / last 30 / custom (based on timestamps if available; otherwise run index)
   - KPI cards should include deltas vs previous window:
     * Avg score (Δ)
     * Median cost (Δ)
     * Tokens p90 (Δ)
     * Accept rate (Δ)
     * Reliability (duration p95, error rate) if captured

4) Clarify COST with real units and optional environmental context:
   - Display cost as currency with a $ label (e.g., “$0.13 avg”).
   - Add optional “environmental estimates” next to cost:
     * 💧 Water (liters) and ⚡ energy (kWh) as derived estimates
   - IMPORTANT: do NOT hardcode questionable constants.
     * Implement conversions as configurable constants in one place (e.g., /config/telemetry.ts):
       COST_PER_DOLLAR_WATER_LITERS (default null/disabled)
       COST_PER_DOLLAR_ENERGY_KWH (default null/disabled)
     * If conversion factors are not set, show “Env: not configured” with tooltip explaining how to enable.

5) Fix outcome counting bug and data quality callouts:
   - Ensure Accepted/Refine/Rejected/Unknown counts sum to runsTracked.
   - If “unknown” is being derived incorrectly, fix the mapping.
   - Add a small “Data Quality” line:
     * “Duration captured: X/Y runs”
     * “Agents captured: X/Y runs”
     * “Steps captured: X/Y runs”
     (so users understand why some analytics show “Not captured”.)

6) Add “Insights” (actionable recommendations) panel:
   - Generate 3–5 deterministic insights from the data, e.g.:
     * “High-cost low-score outliers: runIds …”
     * “Best efficiency (score per $): runId …”
     * “Most improved recent run vs baseline: …”
   - Each insight should link to the run details or filtered run list.

IMPLEMENTATION REQUIREMENTS
- Keep backwards compatibility with older logs.
- Use pure helper functions for:
  - binning + histogram segment building
  - percentile calculation
  - delta calculations between windows
  - outlier detection (simple z-score or IQR is fine)
- Charts should be consistent style with the dark theme.
- If there is an existing chart library, use it; otherwise implement simple SVG/canvas charts (but include axes + tooltips).

DELIVERABLES
- Updated Milestones dashboard layout with the three sections.
- Replaced “distribution bars” with interpretable distributions (histograms/segmented bars).
- Chart axes/units/tooltips everywhere.
- “How scoring works” + Glossary/Methodology UI.
- Cost displayed with currency ($), and optional 💧/⚡ estimates via config constants (disabled by default).
- Fixed outcome counter bug and added data quality summary.
- Deterministic Insights panel with links.

ACCEPTANCE CRITERIA
- A user can look at the dashboard and understand what Score/Cost/Tokens mean within 10 seconds.
- Distributions visually encode bucket proportions (not full-width decorative bars).
- Cost is shown as $ with clear formatting; env metrics are optional + configurable and never misleading.
- Outcome totals and run counts are consistent and correct.

###################################################################
You are updating the Milestones analytics dashboard (current output matches milestones.htm).

GOAL
Upgrade the current “pretty analytics” into a decision-grade dashboard by fixing misleading charts, adding axes/units, and tightening insights so they can’t produce nonsense.

CURRENT ISSUES TO FIX (from the existing UI)
1) Correlation chart is plotting missing data as real points:
   - “Score vs Tokens correlation” includes points with “Tokens n/a” that appear at x=0.
   - It also includes “Score n/a” points.
   REQUIRED: Never plot missing values. Filter them out. Instead, show a small badge: “Excluded due to missing tokens: X” and/or “Excluded due to missing score: Y”.

2) Trend chart lacks readable axes/units and appears index-based:
   - “Score & Cost trend” needs y-axis ticks and units (Score points, Cost USD).
   REQUIRED: Add axis labels and tick marks (at least min/mid/max). Provide toggle:
     - X-axis: Run index (default if timestamps missing)
     - X-axis: Time (if timestamps exist)

3) Insight math must be robust:
   - “Score per dollar” must not divide by zero or near-zero cost.
   REQUIRED: If cost <= 0.005 (or missing), exclude from score/$ rankings and label as “Cost too small to compute score/$ reliably”.
   Add “best efficiency” as: score / max(cost, 0.01) ONLY if explicitly labeled as “normalized”.

4) Run identification/readability:
   - Outliers and links show full filesystem paths. Replace with short RunId + timestamp display.
   REQUIRED: Display:
     - Primary label: RunId (derived from folder name like 20251102-021356)
     - Secondary: timestamp if present
     - Full path only via tooltip + copy button

5) Distribution charts: keep the segmented histograms, but make them self-explanatory:
   - Add x-axis range labels (bin boundaries) under the segmented bar.
   - On hover tooltip must show: bin min/max, count, %, and example run IDs.
   - Keep median/p90 markers, but label them below (e.g., “Median 7.5”, “P90 8.0”).

DELIVERABLES
- Updated correlation component that filters missing values + shows excluded counters.
- Updated trend chart with axes, units, and X-axis toggle (index vs time).
- Hardened Insights computations (no divide-by-zero; handle missing).
- Run label formatter (short id + hover/copy path).
- Distribution bin boundary labels + richer tooltips.

ACCEPTANCE
- No chart includes “n/a” points.
- Axes/units are visible without hovering.
- Insights never output absurd values due to $0.00 or missing cost.
- Outliers list is scannable in 2 seconds.

########################################################
You are working in the Unified AI Toolbox web app. Update the Agent Library page so the agent’s actual model prompt is visible, auditable, and (optionally) editable. The agent prompt must come from the Agent Library JSON and match exactly what the orchestrator would send to the model.

GOALS
1) Make the agent prompt a first-class field on the Agent Detail view:
   - Add a “Prompt” section (accordion/collapsible by default).
   - Show the full prompt text from the library (e.g., agent.prompt).
   - Provide buttons:
     * Copy Prompt (copies exact text)
     * Expand/Collapse
     * Show Diff (against the canonical library prompt)
   - If the UI supports editing, add:
     * Edit toggle (off by default)
     * Save Override (persist override locally or via backend)
     * Reset to Library (discard override)

2) Prevent “prompt drift”:
   - Establish a clear precedence rule:
     - Canonical Prompt = agent.prompt from the library JSON.
     - Optional Override Prompt = user-edited prompt stored separately.
   - Add a “Prompt Source” indicator:
     - “Library” if using canonical
     - “Override” if overridden
   - If there’s any generated or wrapped prompt logic, add a “Rendered Prompt Preview” section:
     - Displays the exact final prompt that will be sent to the model at runtime.
     - Include any wrappers, headers, or injected context.
     - Include a checksum/hash (simple SHA-256 or stable hash) for both canonical and rendered prompts.

3) Improve usability and clarity:
   - Add tooltips/definitions:
     - “This is the exact instruction block provided to the model for this agent.”
     - Explain “Library prompt vs Override vs Rendered prompt.”
   - Add a quick search inside the prompt (find-in-text).
   - Keep the page clean (dark theme). Prompt section should not dominate the layout.

DATA REQUIREMENTS
- Agents are loaded from the Agent Library JSON (agent-library.json). Use agent.prompt as the canonical prompt.
- If agent.prompt is missing, show: “Prompt not present in library data for this agent.”
- Implement overrides without mutating the original library JSON in-memory:
  - Store overrides in a separate structure keyed by agentId/name (e.g., localStorage “agentPromptOverrides” OR backend endpoint if available).
  - On load: display canonical prompt, then apply override if present.

DELIVERABLES
- Updated Agent Library detail component with:
  - Prompt accordion
  - Copy button
  - Diff view (minimal line-based diff is fine; do not add heavy dependencies unless already present)
  - Optional editing + override persistence
  - Rendered prompt preview (if runtime uses wrappers/injected context)
  - Prompt source indicator + hash/checksum display
- Ensure zero breaking changes for existing agent cards and metadata views.

ACCEPTANCE CRITERIA
- For any agent, the page clearly shows the exact prompt from the library.
- Copy produces the exact prompt text (no formatting changes).
- If an override exists, the UI clearly indicates it and can reset to library.
- Rendered prompt preview (if applicable) matches what runtime would send.
- Missing prompt fields fail gracefully without UI errors.
